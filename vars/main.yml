---
# vars file for rust
rust_name : 'rust-{{rust_ver}}-{{rust_platform}}'
rust_tgz : '{{rust_name}}.tar.gz'

# https://static.rust-lang.org/dist/rust-1.12.0-x86_64-unknown-linux-gnu.tar.gz
rust_url : '{{rust_mirror}}/{{rust_tgz}}'

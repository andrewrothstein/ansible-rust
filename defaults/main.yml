---
# defaults file for rust
rustup_mirror: https://static.rust-lang.org/rustup/dist
rustup_platform: '{{ ansible_architecture }}-unknown-linux-gnu'

rustup_toolchains:
  - stable
  - beta

rustup_default_toolchain: stable

rustup_home: '{{ ansible_env.HOME }}/.rustup'
